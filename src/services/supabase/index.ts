export { supabase } from './client';
export { secureClient } from './secureClient';

// Helper functions will be added in Authentication phase
// export { signIn, signOut, getSession, onAuthStateChange } from './auth';