# Sinoman Mobile App - Environment Variables
#
# Instructions:
# 1. Copy this file to .env
# 2. Fill in the actual values
# 3. Never commit .env to git
#
# For production builds, use EAS Secrets instead

# Supabase Configuration (shared dengan PWA)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# API Configuration (optional, jika berbeda dari Supabase)
EXPO_PUBLIC_API_URL=https://api.sinoman.co.id

# Environment
EXPO_PUBLIC_ENV=development

# Feature Flags (untuk nanti)
EXPO_PUBLIC_ENABLE_ANALYTICS=false
EXPO_PUBLIC_ENABLE_CRASHLYTICS=false

# Monitoring & Error Tracking
EXPO_PUBLIC_SENTRY_DSN=https://your-key@sentry.io/your-project-id
EXPO_PUBLIC_SENTRY_RELEASE=1.0.0

# Expo EAS Configuration
# Get this ID by running: eas init
# IMPORTANT: Also add this to app.json under expo.extra.eas.projectId
EAS_PROJECT_ID=your-expo-project-id-here

# Firebase Configuration Files (NOT in .env)
# These are separate JSON/plist files, not environment variables:
# - google-services.json (Android) - place in project root
# - GoogleService-Info.plist (iOS) - place in project root
# Download from Firebase Console > Project Settings > Your apps
# These files are gitignored for security. Use template files as reference.